# text to speech
realtimetts[kokoro,coqui,orpheus]==0.5.5

# webserver dependencies
fastapi
uvicorn[standard]

# configuration
python-dotenv

# llm providers
ollama
openai

# speech to text (Vosk for real-time streaming)
vosk
numpy

# data validation
pydantic

# transformers is required by Coqui XTTS, using default bundled version

# database
SQLAlchemy